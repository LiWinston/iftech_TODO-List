spring.application.name=tdl

# --- Postgres ---
spring.datasource.url=jdbc:postgresql://localhost:5432/todolist
spring.datasource.username=postgres
spring.datasource.password=lyc980820
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# Enable SQL logging during development (optional)
logging.level.org.hibernate.SQL=warn
logging.level.org.hibernate.orm.jdbc.bind=warn

# --- Redis (Redisson) ---
# 我们将手动配置 RedissonClient，以支持 rediss TLS + ACL 用户名/密码

# --- App specific ---
tdl.redis.url=rediss://red-croo37l6l47c73fqrus0:ww41TwtRqH8XFcvPOI0IogGwSSTmp9Ij@singapore-keyvalue.render.com:6379

# Embedding model dimension (all-MiniLM-L6-v2 -> 384)
tdl.embedding.dimension=384

# Delayed delete seconds (e.g. 7 days)
tdl.delete.delay-seconds=604800

